	- build GameState and Sprite structures around chipmunk data structures (make it an implementation detail)
	- create a data structure to hold SDL_Surface + animations, this data structure should depend on GameState/Sprite (not the other way around)
